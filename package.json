{
  "name": "ai-doc-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "python app.py",
    "dev:frontend": "python app.py frontend",
    "dev:backend": "python app.py backend",
    "build": "npm --prefix docuagent-frontend run build",
    "preview": "npm --prefix docuagent-frontend run preview",
    "test": "npm --prefix docuagent-frontend run test:run"
  }
}
